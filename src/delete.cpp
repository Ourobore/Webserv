#include "Webserv.hpp"

void Webserv::handle_delete(Config& server_config, Request& request,
                            ClientHandler& client)
{
    (void)server_config;
    (void)request;
    (void)client;
}