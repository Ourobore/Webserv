{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "build",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/server",
            "args": [],
            "cwd": "${workspaceFolder}",
            //"preLaunchTask": "clang build",
            "initCommands": [
                "settings set target.disable-aslr false"
            ]
        }
    ]
}